{
  "name": "disgustometer",
  "version": "1.0.0",
  "private": true,
  "description": "מד הגועל — voice-driven disgust meter (React + Express + Anthropic)",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c green,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
